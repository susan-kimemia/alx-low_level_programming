#include <stdio.h>

/**
 * main - printing name and then new line
 * @argc: arguments
 * @argv: array
 * Return: zero
 */
	int main(int argc __attribute__((unused)), char *argv[])
	{
	printf("%s\n", argv[0]);
return (0);
}

